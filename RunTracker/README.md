# Run Tracker

A small library to use from my mods. Gives signals when a run on the island is started and when the players go back to the airport. This isn't meant to be a separate mod/dependency for the user, its dll is bundled with the mod's dll. This is to help me avoid duplication code between mods
